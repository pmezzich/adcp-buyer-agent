"""adcp-buyer-agent: an AdCP buyer agent and a fuzzer for AdCP sales agents.

Package layout is filled in by the implementation phase; this module exists so the
project builds and dependencies resolve.
"""

__version__ = "0.0.1"
